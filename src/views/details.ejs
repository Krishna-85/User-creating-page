<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Cards</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/flowbite@2.5.2/dist/flowbite.min.css" rel="stylesheet" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        html, body {
            width: 100%;
            height: 100%;
        }
        .card {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 1.5rem;
            background: #1f2937; /* Dark Gray for better readability */
            text-align: center;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            width: 250px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .card img {
            border-radius: 50%;
            height: 7rem;
            width: 7rem;
            object-fit: cover;
            margin-bottom: 1rem;
        }
        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>
<body class="bg-gray-100">

    <!-- Navbar -->
    <nav class="bg-white border-gray-200 dark:bg-gray-900">
        <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
          
            <a href="/" class="flex items-center space-x-3 rtl:space-x-reverse">
            
                <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">📜Create User</span>
               
            </a>
            <div class="flex md:order-2">
                <div class="relative hidden md:block">
                    <input type="text" id="search-navbar" 
                        class="block w-full p-2 ps-10 text-sm text-gray-900 border rounded-lg bg-gray-50 focus:ring-blue-500 dark:bg-gray-700 dark:text-white" 
                        placeholder="Search..." />
                </div>
                <button type="button" 
                    class="inline-flex items-center p-2 text-gray-500 rounded-lg md:hidden hover:bg-gray-100 dark:hover:bg-gray-700">
                    <svg class="w-5 h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
                    </svg>
                </button>
            </div>
        </div>
    </nav>

    <!-- Cards Section -->
    <div class="flex flex-wrap justify-center items-center gap-6 p-6">
        
        <div class="card">

            <img  src="<%= userdata.imageUrl %>" alt="User Image" />
            <a href="/details/<%= userdata._id %>" class="text-xl text-white font-semibold"><%= userdata.username %></p>
            <p class="text-sm text-gray-400"><%= userdata.email %></p>
            
          </div>
      
    </div>
   
    <script src="https://cdn.jsdelivr.net/npm/flowbite@2.5.2/dist/flowbite.min.js"></script>
</body>
</html>